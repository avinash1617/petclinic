apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Maven Build
        kind: build
        uses: docker://maven:3.9.3-openjdk-17
        run: mvn clean install
